<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Client Action for Grid Dashboard -->
    <record id="action_occupancy_grid_dashboard" model="ir.actions.client">
        <field name="name">Location Occupancy Grid</field>
        <field name="tag">occupancy_grid_dashboard</field>
        <field name="target">current</field>
    </record>

    <!-- Menu Item for Grid Dashboard -->
    <menuitem 
        id="menu_occupancy_grid_dashboard"
        name="Occupancy Grid 📊"
        parent="menu_location_occupancy_root"
        action="action_occupancy_grid_dashboard"
        sequence="5"/>

</odoo>
